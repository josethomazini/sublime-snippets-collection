<snippet>
    <content><![CDATA[
def save(self, commit=True):
    instance = super(${1:CLASS_NAME}, self).save(commit=False)

    if commit:
        instance.save()
    return instance
]]></content>
    <tabTrigger>ssc_django_super_save</tabTrigger>
    <scope>source.python</scope>
</snippet>
